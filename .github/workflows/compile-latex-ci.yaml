name: Compile Course Notes

on:
  push:
    branches:
      - master
      - main

permissions:
  contents: write

jobs:
  build:
    uses: RiccardoElena/UniNotes_Template/.github/workflows/latex-compile.yaml@master
    with:
      pdf_name: 'Logic_for_Computer_Science'
